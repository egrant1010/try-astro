---
export interface Props {
    title: string,
    classes?: string
}

const { title, classes } = Astro.props;
---

<nav class={`top-nav container mx-auto bg-emerald-800 text-white text-2xl p-2 ${classes}`}>
	<h1 class="font-bold">{title}</h1>
</nav>
